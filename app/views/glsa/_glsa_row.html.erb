<tr class="<%= cycle 'even', 'odd' %>">
  <td><strong><tt><%= h glsa.glsa_id %></tt></strong></td>
  <td>
    <%= status_icon glsa.status %>
    <%= bugready_icon false %>
    <%= approval_icon glsa.approval_status %>
    <%= workflow_icon(glsa.workflow_status(current_user)) %>
  </td>
  <% if view == :requests %>
    <td><strong><%= link_to(h(glsa.last_revision.title), {:action => 'edit', :id => glsa}, {:title => "Draft this GLSA"}) %></strong></td>
    <td><%= glsa.created_at %>, <%= h glsa.requester.name %> (<em><%= h glsa.requester.login %></em>)</td>
  <% else %>
    <td><strong><%= link_to(h(glsa.last_revision.title), {:action => 'show', :id => glsa}, {:title => "Show this GLSA"}) %></strong></td>
    <td><%= glsa.created_at %>, <%= h glsa.submitter.name %> (<em><%= h glsa.submitter.login %></em>)</td>  
  <% end %>
  <td>
    <% if glsa.last_revision; glsa.last_revision.bugs.each do |b| %>
      <%= link_to_function h(b.bug_id), "buginfo('#{h b.bug_id}')" %>
    <% end ; end %>
  </td>
  <% if current_user.is_el_jefe? %>
  <td>
    <%= image_tag 'icons/delete.png' %>
  </td>
  <% end %>
</tr>